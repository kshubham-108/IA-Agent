# IA template guide

Template-specific overlay vectors, complexity tables, and management overlays live in `packages/rules/` YAML — never assume defaults.

RetainX, O2T, DFE, Data ETL, MW, 360 AD each have distinct anatomy. Call the matching `estimate_*` tool.

Every line item must trace to a rule id and evidence span.
