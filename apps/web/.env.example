# Next.js web app — copy to .env.local in this directory
#
# When AGENT_API_URL is unset, the UI uses the in-process mock agent client.

# Deployed Eve agent HTTP endpoint (e.g. http://localhost:3000 from `eve dev`)
AGENT_API_URL=
