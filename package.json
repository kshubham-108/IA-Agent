{
  "name": "ia-agent",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test && pnpm test:root",
    "test:root": "vitest run tests",
    "test:leak-check": "vitest run tests/knowledge-leak.test.ts",
    "typecheck": "pnpm -r run typecheck",
    "eval": "EVE_MOCK_MODEL=1 eve eval",
    "eval:live": "eve eval",
    "ingest-corpus": "tsx corpus/scripts/ingest.ts",
    "calibrate": "tsx evals/run-calibration.ts",
    "bakeoff": "tsx evals/bakeoff.ts"
  },
  "dependencies": {
    "@ai-sdk/gateway": "^4.0.25",
    "@ia-agent/engine": "workspace:*",
    "@ia-agent/extraction-schemas": "workspace:*",
    "ai": "^7.0.33",
    "eve": "0.26.2",
    "yaml": "^2.8.0",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@types/node": "24.x",
    "mammoth": "^1.9.0",
    "pdf-parse": "^1.1.1",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4",
    "xlsx": "^0.18.5"
  },
  "packageManager": "pnpm@10.12.1",
  "engines": {
    "node": "24.x"
  }
}
